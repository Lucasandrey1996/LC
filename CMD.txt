vitesse du processuer en Hz
	vcgencmd measure_clock arm
ou
	cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq

tension du processeur
	vcgencmd measure_volts

tempörature du processeur
	vcgencmd measure_temp

